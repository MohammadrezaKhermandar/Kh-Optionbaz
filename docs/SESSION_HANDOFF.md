# یادداشت تحویل سشن

سند زنده‌ی «از کجا ادامه بدهیم». **آخرین به‌روزرسانی: ۲۰۲۶-۰۹-۱۸.**

وضعیت‌های زیر در همین تاریخ از git، GitHub و خودِ پایگاه‌ها خوانده
شده‌اند — ولی در شروع هر سشن **دوباره بررسی‌شان کنید**؛ به این سند
به‌عنوان حقیقت جاری اتکا نکنید.

> این سند تاریخچه‌ی همه‌ی سشن‌ها را تکرار نمی‌کند. مراحل تمام‌شده
> (recorder، حسابداری معاملات کاغذی، غربال نقدشوندگی) در README و در
> PRهای مرج‌شده‌ی ۱ تا ۷ مستندند.

---

## ۱. کجا ایستاده‌ایم

**هدف مرحله‌ی جاری:** پس از اینکه غربالِ نقدشوندگی دروازه شد، حالا
کاربر باید بفهمد **کدام** فرصتِ عبورکرده ارزش بررسی بیشتری دارد، چرا
بالاتر نشسته و ضعفش کجاست.

| مورد | مقدار |
|---|---|
| مسیر پروژه | `D:\Users\M.Kharmandar\Desktop\Codes\optionbaz\Kh-Optionbaz` |
| کد | زیرپوشه‌ی `option_signal_bot/` |
| `origin` | `git@github.com:MohammadrezaKhermandar/Kh-Optionbaz.git` (**SSH**، بخش ۶) |
| `origin/main` | **`d24726a`** (merge PR #6) |
| **PR باز** | **#8** — https://github.com/MohammadrezaKhermandar/Kh-Optionbaz/pull/8 |
| شاخه‌ی PR #8 | `feature/opportunity-ranking` → مقصد `main` |
| **SHA بررسی‌شونده** | **`800a4dc5dccfacb4777aaee1b28c3edec4a3ce00`** |
| **CI آن SHA** | **✅ هر ۶ check سبز** — `test (3.11)`، `test (3.12)`، `architecture-guard` در دو اجرا (push و pull_request). بررسی‌شده با API در ۲۰۲۶-۰۹-۱۸. |
| وضعیت PR #8 | `open` · `mergeable: true / clean` · **merged: خیر** · ۴ کامیت · ۱۳ فایل · ‎+۲۰۶۰/−۱۴ |
| شاخه‌ی همین سند | `docs/handoff-2026-09-12` — `main` در آن merge شده، پس checkoutش هم کدِ `main` را می‌دهد هم این سند |

### PRهای بسته‌شده‌ی این دوره

| PR | چه بود | نتیجه |
|---|---|---|
| **#6** | غربال قابلیت معامله + گزارش پژوهش | **merge شد** روی `5f58286`ِ تأییدشده‌ی کدکس → merge commit `d24726a` |
| **#7** | رفع وابستگی تست‌ها به تاریخ واقعی | **merge شد** → `3f8b8ad` |

PR #7 از دلِ همین سشن درآمد: ۶ تست در `tests/test_web_api.py` به تاریخِ
واقعیِ سیستم گره خورده بودند (فیکسچرِ `ضهرم6040` سررسیدش `2026-09-16`
بود). **روی `main`ِ دست‌نخورده هم بازتولید شد**، یعنی به هیچ PRی ربط
نداشت. حالا زمان در آن تست‌ها منجمد می‌شود و یک تستِ مستقل هم سناریوی
سررسیدشده را می‌سنجد.

**⚠️ هیچ‌چیز merge نشده‌ای نمانده که منتظرش باشید جز PR #8.**

---

## ۲. بازبینی پنجم کدکس روی PR #8 — شش مورد

همه در کامیت `800a4dc` پاسخ گرفتند. **این‌ها اصلاح‌شده و توسط اپوس
بررسی شده‌اند، ولی هنوز بازبینیِ نهاییِ کدکس را نخورده‌اند** (بخش ۳).

### ۲.۱ هزینه‌ی رفت‌وبرگشت از قیمتِ اجراپذیر

| مورد | مقدار |
|---|---|
| محل | `market/tradability.py::LiquidityObservation.round_trip_cost_pct` · `tradability_screener.py::_exit_book` (حالا سمتِ ورود را هم می‌خواند) · `opportunity_ranking.py::_round_trip_cost_component` |
| تست | `test_round_trip_cost_uses_both_executable_sides_not_half_the_spread` · `test_round_trip_cost_is_unknown_when_a_side_cannot_be_executed` |
| مثال عددی | دفتر ۹۸۰/۱۰۲۰ برای ۱۰ قرارداد: **قبل ۲٫۰۰٪** (نصف اسپرد) → **بعد ۳٫۹۲٪** |

نصفِ اسپرد هزینه‌ی رفت‌وبرگشت نبود. حالا میانگین وزنیِ پرشدنِ **هر دو
سمت** برای **همان تعداد قرارداد** خوانده می‌شود: `(ورود − خروج) ÷ ورود`،
با واحد «٪ از پرمیومِ پرداختی». سمتی که برای این حجم اجراپذیر نباشد
⇒ **نامعلوم**، بدون عددسازی.

### ۲.۲ اتصال با شناسه‌ی سیگنال، نه با نماد

| مورد | مقدار |
|---|---|
| محل | `market/tradability.py::ScreeningRecord.signal_id` · `signals/signal_generator.py::_is_tradable` · `web/api.py::_build_ranking` (`by_signal_id`) |
| تست | `test_screening_records_carry_the_signal_id_they_belong_to` · `test_rejected_records_keep_their_verdict_and_reason_in_the_exclusions` |

روی یک نماد می‌تواند چند سیگنال از چند استراتژی با سمت و تعدادِ
متفاوت در یک پاس باشد؛ اتصال با نماد یعنی نتیجه‌ی غربالِ یکی به دیگری
بچسبد. رکوردهای `rejected`/`needs_review` هم که اصلاً سیگنالِ
منتشرشده ندارند، **با حکم و علتِ خودشان** در کنارگذاشته‌ها می‌مانند.

### ۲.۳ دامنه: فقط خریدِ اختیارِ تک‌پایه

| مورد | مقدار |
|---|---|
| محل | `opportunity_ranking.py::rank_opportunities` — دروازه‌ی سوم |
| تست | `test_a_sell_position_is_not_scored_with_the_buy_formula` · `test_a_multi_leg_leg_is_excluded_with_a_stated_scope_reason` |
| مثال | رتبه‌گرفته = `[خرید]`؛ فروش و چندپایه با علتِ صریح کنار رفتند |

سرمایه‌ی درگیر، سر‌به‌سر و حداکثر زیانِ این نسخه همه از منطقِ **خرید**
می‌آیند و برای فروش معتبر نیستند. مدل‌سازیِ درستِ فروش/پوششی کارِ این
PR نیست.

### ۲.۴ تفکیک زیان‌ها و سازگاریِ پول

| مورد | مقدار |
|---|---|
| محل | `RankedOpportunity.max_theoretical_loss` / `stop_loss_loss` / `capital_required` / `breakeven_includes_fees` · `breakeven_price(..., fee_per_unit)` |
| تست | `test_stop_loss_loss_is_not_presented_as_the_theoretical_maximum` · `test_capital_and_breakeven_come_from_the_executable_entry_price` · `test_a_breakeven_without_fees_is_not_presented_as_net` · `test_declared_zero_fees_differ_from_an_unknown_rate` |
| مثال عددی | ورود ۱۰۲۰ × ۱۰ × ۱۰۰۰ + کارمزد ۵۰٬۰۰۰ → سرمایه ۱۰٬۲۵۰٬۰۰۰ · **حداکثر زیان نظری ۱۰٬۲۵۰٬۰۰۰** (کل پرمیوم) · **زیان تا حد ضرر ۳٬۵۰۰٬۰۰۰** · سر‌به‌سر ۱۱٬۰۲۵ (با کارمزد) |

`max_loss` ماژول ریسک «زیان تا حد ضرر» است، ولی رابط «بیشترین زیان»
نشانش می‌داد. حالا جدا گزارش می‌شوند. همه‌ی عددهای ریالی از **قیمتِ
اجراپذیرِ ورود** می‌آیند تا با هم بخوانند، و سر‌به‌سرِ بدون کارمزد
صریحاً «خالص نیست» برچسب می‌خورد.

**صفرِ اعلام‌شده ≠ نرخِ نامعلوم:** اولی هزینه‌ی دانسته و امتیازِ کامل
(پوشش ۱۰۰٪)، دومی نامعلوم و بی‌امتیاز (پوشش ۹۰٪، امتیاز کل ۹۱٫۱ در
برابر ۸۱٫۱ با سقفِ ۹۱٫۱).

### ۲.۵ مؤلفه‌ی عملکرد استراتژی حذف شد

| مورد | مقدار |
|---|---|
| محل | `opportunity_ranking.py::EVIDENCE_DISABLED_NOTE` — مؤلفه اصلاً ساخته نمی‌شود |
| تست | `test_the_payload_states_the_scope_and_the_disabled_evidence` |

تنها دادهٔ موجود نرخ برد بود، و نرخ برد به‌تنهایی شاهدِ عملکرد نیست:
چند بردِ کوچک و یک باختِ بزرگ هم نرخ بردِ بالا می‌سازد. تا وقتی سود و
زیانِ **پس از هزینه** با مبنای قابل مقایسه ثبت نشود، این مؤلفه امتیاز
نمی‌دهد. علتش در پاسخ API و بالای کارتِ رابط نوشته می‌شود.

**شاهدِ تجربی:** `var/signals.db` همین حالا **صفر** سیگنال و صفر
`signal_outcomes` دارد — یعنی حتی اگر مؤلفه فعال بود، چیزی برای
سنجیدن نبود.

### ۲.۶ ظرفیت خروج فقط درونِ محدوده‌ی قیمتی

| مورد | مقدار |
|---|---|
| محل | `data/order_book.py::OrderBook.depth_within` · `LiquidityObservation.usable_exit_depth_ratio` · `_exit_capacity_component` |
| تست | `test_far_away_volume_is_not_counted_as_exit_head_room` · `test_a_zero_depth_floor_does_not_silently_disable_the_capacity_measure` |
| مثال عددی | دفتر `۱۰@۱۰۰۰ + ۱۰۰۰@۷۰۰`: عمقِ کل **۱۰۱ برابرِ سفارش** → امتیاز کامل (قبل) · درونِ محدوده **۱٫۰۰ برابر** → **امتیاز ۰** (بعد). دفتر `۱۰@۱۰۰۰ + ۲۰@۹۶۰`: ۳٫۰۰ برابر → امتیاز ۱۰۰ |

حجمی که ۳۰٪ پایین‌تر از مظنه نشسته راهِ خروج نیست. **دروازه‌ی غربال
همچنان با عمقِ کل کار می‌کند** (دامنه‌ی PR #6، تأییدشده)؛ فقط سنجه‌ی
حاشیه‌ی اطمینانِ رتبه‌بندی باندی شد.

اصلاحِ «کفِ عمقِ صفر» (کامیت `076663f`) هم نگه داشته و تست شد: اگر
کاربر کفِ غربال را صفر بگذارد، ضرب‌کردن هم صفر می‌شد و سنجه بی‌صدا از
کار می‌افتاد.

---

## ۳. اصلاح‌شده در برابر منتظرِ بازبینی — تفکیک صریح

### ✅ اصلاح‌شده و **بررسی‌شده توسط اپوس**

- شش موردِ بخش ۲، هر کدام با تست هدفمند و مثال عددی.
- کلِ مجموعه‌ی تست محلی سبز، `ruff` پاس، و **CI روی `800a4dc` شش‌تا
  سبز** (بررسی‌شده با API، نه حدس).
- رفتارِ رابط با اسکرین‌شات دیده شد: عددهای کارتِ نمونه با هم می‌خوانند.

### ⏳ **هنوز منتظر بازبینی نهاییِ کدکس**

- **کلِ PR #8.** کدکس هنوز نسخه‌ی `800a4dc` را ندیده است. آنچه در
  بخش ۲ آمده خودارزیابیِ اپوس است، نه تأییدِ کدکس.
- تا تعیین تکلیفِ PR #8، **مرحله‌ی بعدِ محصول (بخش ۷) را شروع نکنید.**

### ⚠️ آنچه عمداً انجام **نشد**

مدل پیش‌بینی، اتصال اخبار، LLM، اجرای واقعی سفارش، و هیچ زیرساخت
عمومی برای آینده. برای احتمال موفقیت یا هزینه‌ی نامعلوم هیچ عددی
ساخته نشد.

---

## ۴. وضعیت محلی

**تغییرات کامیت‌نشده: هیچ.** درخت کاری تمیز، `git stash` خالی. همه‌ی
کامیت‌ها push شده‌اند.

**پردازه‌ی در حال اجرا: هیچ.** هیچ `python.exe` در اجرا نیست و پورت‌های
۸۰۷۷/۸۰۷۸/۸۰۷۹ باز نیستند (سرورِ نمایشیِ این سشن بسته شد).

**شاخه‌ی checkout‌شده در پایان سشن:** `docs/handoff-2026-09-12`. برای
کارِ روی قابلیت، `feature/opportunity-ranking` را بگیرید.

**تست‌ها:** کلِ مجموعه روی `800a4dc` محلی سبز است و `ruff` پاس.

### پایگاه‌ها — ⚠️ هنوز هیچ تاریخچه‌ی معتبری نداریم

| پایگاه | وضعیت |
|---|---|
| `var/recorder/market.db` | integrity ok · **۱ snapshot، ۱۴۳۲ قرارداد، فقط `2026-09-11T11:45:58+03:30`** · `source_time` = `NULL` |
| `var/paper_trading.db` | حساب دست‌نخورده · صفر سفارش، صفر موقعیت، صفر معامله |
| `var/signals.db` | **صفر سیگنال، صفر `signal_outcomes`** |
| پشتیبان‌ها | `var/backup-20260911T224029/` و سه پوشه‌ی `var/recorder/backup-2026*` — ⚠️ بدون اجازه‌ی محمد حذف نشوند |
| پایگاه‌های قدیمیِ recorder | `market-schema-v1.db`، `market-schema-v2-inscode.db`، `test-schema-v1.db`، `test-schema-v2-key.db` — دست‌نخورده |

**آخرین جلسه‌ی ثبت‌شده: ۲۰۲۶-۰۹-۱۱ که جمعه بود** (`weekday=4`) — یعنی
روز **غیرمعاملاتی**. پس از فیلترِ تقویم، تعداد جلسه‌های معاملاتیِ
تأییدشده **صفر** است.

نتیجه‌ی عملی، که باید صریح بماند: **سنجه‌ی «تداوم معامله» همیشه
«نامعلوم» است، پس غربال هیچ گزینه‌ای را `tradable` نمی‌کند و رتبه‌بندی
روی دادهٔ واقعی همیشه فهرستِ خالی می‌دهد.** این خرابی نیست — رفتارِ
درست است — ولی یعنی تا ساختنِ تاریخچه، خروجیِ واقعیِ محصول دیده
نمی‌شود. برای دیدنِ شکلِ خروجی `/api/ranking/demo` هست (برچسب‌دار،
مسیر جدا، `demo: true`).

`config/settings.yaml` **وجود ندارد** — پیش‌فرض‌های `config/loader.py`
اعمال می‌شوند.

---

## ۵. محدودیت‌های باقی‌مانده

۱. **پژوهشِ استراتژی‌های بازار ایران ناتمام است.** هیچ پژوهشِ عملکردی
   با روش و داده درباره‌ی بازدهی استراتژی‌های اختیار در ایران پیدا
   نشد (فارسی و انگلیسی، شامل `jfr.ut.ac.ir`، `ensani.ir`، `sid.ir`،
   `civilica.com`). آنچه فراوان است آموزش و تبلیغ است، نه شاهد.
   **نبودِ شاهد ≠ ردِ استراتژی** — ولی یعنی هیچ آستانه و وزنی در این
   پروژه پشتوانه‌ی تجربی ندارد.

۲. **اعتبار جاریِ دستورالعمل تأیید نشده است.** نسخه‌ی خوانده‌شده مصوب
   ۱۳۹۱/۱۲/۱۳ با اصلاح ۱۳۹۵/۰۹/۱۷ است و روی صفحه‌ی رسمی فرابورس
   منتشر است، ولی `cmr.seo.ir` و `tse.ir` پاسخ قابلِ واکشی ندادند
   (تلاش دوباره در ۲۰۲۶-۰۹-۱۷ هم همان نتیجه). جست‌وجوی وب هیچ
   اصلاحیه‌ای بعد از ۱۳۹۵/۰۹/۱۷ نشان نداد — **شاهدِ ثانویه، نه تأییدِ
   اولیه**. اگر اصلاحیه‌ای باشد، در اسناد ما نیست.

۳. **مشخصات قرارداد نمادهای هدف استعلام نشد** (`ضخود`، `ضهرم` و …):
   درصدهای `A`/`B` وجه تضمین، سبک اعمال و کارمزدشان. تنها چیزِ
   تأییدشده این است که اندازه‌ی قرارداد یکسان نیست (۱۰۰۰ در برابر
   ۱۳۷۱).

۴. **آستانه‌ها و وزن‌ها فرضِ اولیه‌اند**، نه نتیجه‌ی پژوهش یا
   بهینه‌سازی — و هیچ‌جا طورِ دیگری معرفی نشده‌اند.

۵. **رتبه‌بندی فقط در پاس رصد محاسبه می‌شود.** سیگنالِ قدیمی مظنه‌ی
   لحظه‌ای ندارد، پس رتبه‌دادن به آن یعنی عددسازی.

جزئیات کامل: [`docs/strategy-research.md`](strategy-research.md) بخش ۸.

---

## ۶. محیط (نکاتی که وقت می‌گیرند)

- مفسر: `D:\Users\M.Kharmandar\AppData\Local\Programs\Python\Python313\python.exe`
  (۳.۱۳.۴، روی PATH). `.venv` وجود ندارد و لازم هم نیست.
- **git با SSH کار می‌کند.** توکنِ HTTPS وسط سشنِ قبل منقضی شد (۴۰۱)؛
  محمد کلید `W:/.ssh/id_ed25519` را روی حسابش ثبت کرد و `origin` به
  SSH منتقل شد. `ssh -T git@github.com` برای بررسی.
- **REST API** (برای PR و merge) با `git credential fill` + توکن کار
  می‌کند؛ `gh` نصب نیست. مخزن **عمومی** است، پس خواندنِ وضعیت PR و CI
  بدون توکن هم ممکن است — ولی سقفش ۶۰ درخواست در ساعت است و پولینگِ
  طولانی به آن می‌خورد. برای پولینگ از مسیر احراز‌هویت‌شده استفاده کنید.
- ⚠️ **CI این مخزن حدود ۲۶ دقیقه طول می‌کشد** (روی سه SHA آخر یکسان).
  `architecture-guard` در ۳۰ ثانیه تمام می‌شود ولی `test` ها طولانی‌اند؛
  تست‌های مبتنی بر `/api/scan` سهم عمده دارند. **قبل از اینکه فکر کنید
  CI گیر کرده، زمان‌های SHA قبلی را مقایسه کنید.**
- `ruff` نصب نیست؛ venv جدا در scratchpad سشن بسازید.
- خروجی فارسی: همیشه `PYTHONIOENCODING=utf-8` (بدون آن `print` فارسی
  با `UnicodeEncodeError` می‌افتد).
- خروجی `pytest` را به فایل بفرستید؛ فیلتر RTK گاهی «No tests
  collected» نشان می‌دهد در حالی که تست‌ها پاس شده‌اند.
- در Git Bash، آرگومانی که با `/` شروع شود به مسیر ویندوزی تبدیل
  می‌شود (`/pulls/8` → `C:/Program Files/Git/pulls/8`). مسیرِ API را
  داخل خودِ اسکریپت بسازید، نه از آرگومان.
- برای `grep`/`wc` مسیرها را با `/` بدهید؛ مسیرِ ویندوزیِ `\` در bash
  بی‌صدا هیچ‌چیز match نمی‌کند.
- ⚠️ **خط‌پایان:** `option_signal_bot/README.md` و
  `data/tsetmc_option_chain_client.py` در git با **CRLF** ذخیره‌اند.
  نوشتنشان با `newline="\n"` کل فایل را در diff عوض می‌کند. راه‌حل:
  با `\r\n` بنویسید یا `git -c core.autocrlf=false add` کنید.
  **این راه‌حل فقط برای همین دو فایلِ استثناست.** بقیه‌ی فایل‌ها در git
  با **LF** ذخیره‌اند و `git add` معمولی دیفِ تمیز می‌دهد؛ زدنِ
  `-c core.autocrlf=false` روی آن‌ها برعکس، دیف را کاذباً کل‌فایل
  می‌کند. قبل از commit همیشه `git diff --stat` را نگاه کنید.

**اجرای داشبورد:**

```powershell
cd D:\Users\M.Kharmandar\Desktop\Codes\optionbaz\Kh-Optionbaz\option_signal_bot
python -m web
```

تب «سیگنال‌ها» → کارتِ «رتبه‌بندی اولویت بررسی». `?ranking=demo` در
URL، نمونه‌ی برچسب‌دار را باز می‌کند.

---

## ۷. جهتِ محصول

ترتیب عمدی است؛ هر مرحله روی مرحله‌ی قبل سوار است:

```
۱. غربال نقدشوندگی            ✅ merge شد (PR #6)
        ↓
۲. رتبه‌بندی توضیح‌پذیر         🔄 PR #8، منتظر بازبینی کدکس
        ↓
۳. تحلیل وضعیت بازار و سهم تا سررسید + اخبار محلی   ⛔ شروع نشده
        ↓
۴. ارزیابی با معاملات کاغذی    ⛔ شروع نشده
```

**چرا این ترتیب:** رتبه‌بندی بدون غربال یعنی امتیازدادن به قراردادی که
نمی‌شود از آن خارج شد. تحلیل وضعیت و اخبار بدون رتبه‌بندی یعنی
سیگنالِ بیشتر بدون معیارِ اولویت. و ارزیابی با معاملات کاغذی **آخر**
می‌آید چون تنها راهِ ساختنِ همان «سود و زیانِ پس از هزینه»ای است که
مؤلفه‌ی عملکرد (بخش ۲.۵) به آن نیاز دارد — یعنی حلقه از اینجا بسته
می‌شود.

**اولین اقدام جلسه‌ی بعد:** منتظر بازبینی کدکس روی `800a4dc` بمانید.
مرحله‌ی ۳ را شروع نکنید.

اگر بازبینی هنوز نرسیده، مفیدترین کارِ بدون ریسک:

1. **تاریخچه بسازید** — بزرگ‌ترین مانعِ فعلی (بخش ۴). در یک روز
   معاملاتی (شنبه تا چهارشنبه، ۰۹:۰۰–۱۲:۳۰ تهران):

   ```powershell
   cd D:\Users\M.Kharmandar\Desktop\Codes\optionbaz\Kh-Optionbaz\option_signal_bot
   python scripts\record_market.py --loop
   ```

   پس از چند روز `min_history_sessions` (پیش‌فرض ۵) تأمین می‌شود و
   سنجه‌ی تداوم از «نامعلوم» بیرون می‌آید — آن‌وقت برای اولین بار
   خروجیِ واقعیِ رتبه‌بندی دیده می‌شود.

2. اگر دسترسی به `cmr.seo.ir` یا `tse.ir` باز شد، دو موردِ
   تأییدنشده‌ی بخش ۵ را ببندید. **جست‌وجوی تکراری نکنید** — دو بار
   بی‌نتیجه بوده.

---

## ۸. روش همکاری

- **انتخاب روش پیاده‌سازی با اپوس است.** برای انتخاب‌های معمول توقف و
  تأییدخواهی لازم نیست؛ اگر بخشی از درخواست با کد یا داده‌ی واقعی
  سازگار نبود، راه‌حلِ کوچک‌ترِ درست اجرا و دلیلش گزارش شود.
- **کارهای معمولِ Git با اپوس است** — شاخه، کامیت، push، PR، و merge
  وقتی صریح خواسته شده. بازنویسی تاریخچه نه.
- **تست فقط برای خطرهای رفتاریِ همان قابلیت.** پوشش صوری، زیرساختِ
  عمومی برای آینده، و انتزاعِ زودرس ساخته نمی‌شود.
- **عدد ساخته نمی‌شود.** نبودِ داده «نامعلوم» است، نه صفر و نه
  میانگین. هر ادعای عددی باید مبنا و واحدش روشن باشد.
- **گزارشِ پایانی:** نمونه‌های عددی قبل و بعد، SHA و نتیجه‌ی CI همان
  SHA — بررسی‌شده، نه فرض‌شده.
- سند حاضر روی شاخه‌ی جدا کامیت می‌شود تا SHA کدِ بررسی‌شونده دست
  نخورد. **SHA سند با SHA کد قاطی نشود.**

---

## ۹. متن کامل آخرین بازبینی کدکس (روی `6b7432e`)

عیناً و بدون خلاصه‌سازی نگه داشته شده:

> سپس PR #8 را با اختیار انتخاب روش فنی، در همین دامنه اصلاح کن:
>
> ۱. هزینه‌ی قیمتی رفت‌وبرگشت را از قیمت قابل اجرای ورود و خروج برای
> همان تعداد قرارداد حساب کن؛ مخرج و واحد روشن باشد. نصف اسپرد
> هزینه‌ی رفت‌وبرگشت نیست. اگر ورودی لازم موجود نیست، عدد دقیق نساز.
>
> ۲. هر نتیجه‌ی غربال باید به همان سیگنال متصل شود، نه صرفاً همان
> نماد. چند استراتژی/سمت/اندازه روی یک نماد نباید مخلوط شوند. وضعیت
> و علت rejected و needs_review را هم در کنارگذاشته‌ها حفظ کن.
>
> ۳. نسخه‌ی اول رتبه‌بندی را صریحاً به خرید اختیار تک‌پایه محدود کن.
> فروش و ساختارهای پوششی را با فرمول خرید امتیاز نده؛ مدل‌سازی آن‌ها
> فعلاً کار این PR نیست.
>
> ۴. «زیان تا حدضرر» را از «حداکثر زیان نظری» جدا کن. سرمایه‌ی لازم،
> سر‌به‌سر و هزینه‌ها نیز باید تعریف سازگار داشته باشند؛ سر‌به‌سر بدون
> هزینه را خالص معرفی نکن. صفرِ کارمزدِ اعلام‌شده با نرخ نامعلوم
> متفاوت بماند.
>
> ۵. نرخ برد به‌تنهایی امتیاز مثبتِ کارنامه نسازد. برای این نسخه، اگر
> سابقه‌ی معتبر سودوزیان پس از هزینه و مبنای قابل مقایسه نداریم،
> مؤلفه‌ی عملکرد را صریحاً غیرفعال کن؛ ساخت موتور ارزیابی جدید لازم
> نیست.
>
> ۶. اصلاح محلیِ کف عمق صفر را حفظ و بررسی کن. در سنجه‌ی ظرفیت هم
> حجمِ قیمت‌های بسیار دور نباید به‌عنوان حاشیه‌ی امن خروج امتیاز بگیرد.
>
> داده‌ی نمایشی هم از نظر قیمت ورود، دفتر سفارش، ارزش موقعیت و زیان
> با خودش سازگار باشد؛ برچسب آزمایشی جای سازگاری عددها را نمی‌گیرد.
>
> برای همین رفتارها تست هدفمند کافی است. در پایان نمونه‌های عددی قبل
> و بعد، SHA نهایی و CI همان SHA را گزارش بده. PR #8 فعلاً merge
> نشود؛ اخبار و پیش‌بینی را هنوز شروع نکن.
