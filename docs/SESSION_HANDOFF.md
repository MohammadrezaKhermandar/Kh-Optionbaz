# یادداشت تحویل سشن

سند زنده‌ی «از کجا ادامه بدهیم». **آخرین به‌روزرسانی: ۲۰۲۶-۰۹-۱۲.**

وضعیت‌های زیر در همین تاریخ از git، GitHub و خودِ پایگاه‌ها خوانده
شده‌اند — ولی در شروع هر سشن **دوباره بررسی‌شان کنید**؛ به این سند
به‌عنوان حقیقت جاری اتکا نکنید.

> این سند تاریخچه‌ی همه‌ی سشن‌ها را تکرار نمی‌کند. مراحل تمام‌شده
> (recorder، حسابداری معاملات کاغذی) در README و در PRهای مرج‌شده‌ی
> ۱ تا ۵ مستندند.

---

## ۱. کجا ایستاده‌ایم

**هدف مرحله‌ی جاری:** «پژوهش استراتژی‌های مناسب بازار اختیار ایران و
غربال فرصت‌های قابل معامله». یعنی گزینه‌های کم‌نقدشونده پیش از رسیدن به
پیشنهادها کنار بروند، و برای بقیه معلوم باشد چرا مناسب‌اند و آیا با
اندازه‌ی سفارش کاربر امکان ورود و خروج دارند.

| مورد | مقدار |
|---|---|
| مسیر پروژه | `D:\Users\M.Kharmandar\Desktop\Codes\optionbaz\Kh-Optionbaz` |
| کد | زیرپوشه‌ی `option_signal_bot/` |
| `origin` | `https://github.com/MohammadrezaKhermandar/Kh-Optionbaz.git` |
| `origin/main` | `b076b10` (merge PR #5) |
| **PR باز** | **#6** — https://github.com/MohammadrezaKhermandar/Kh-Optionbaz/pull/6 |
| شاخه‌ی PR #6 | `feature/tradability-screener` |
| **آخرین SHA** | **`4faa39ed3a1c58e2d372e56bbe48137be48af403`** |
| **CI آن SHA** | **✅ هر ۶ check سبز** — `test (3.11)`، `test (3.12)`، `architecture-guard` در دو اجرا (push و pull_request) |
| وضعیت PR | `open` · `mergeable: true / clean` · **merged: خیر** · ۲ کامیت · ۱۷ فایل · ‎+۲۶۰۰/−۹ |
| شاخه‌ی همین سند | `docs/handoff-2026-09-12` — روی `4faa39e` بنا شده |

**چرا سند روی شاخه‌ی جدا:** کامیت کردنش داخل PR #6، هم SHA را عوض
می‌کرد (و CI سبزِ گزارش‌شده را باطل) و هم دامنه‌ی PR را گِل‌آلود.
همین شاخه روی `4faa39e` بنا شده، پس checkout آن هم کد فعلی را می‌دهد
هم این سند. **هیچ تاریخچه‌ای بازنویسی یا حذف نشد.**

PRهای ۱ تا ۵ مرج شده‌اند. PR #6 **بازبینی نشده** و منتظر نظر است.

---

## ۲. بازبینی چهارم کدکس — وضعیت هر مورد

بازبینی روی `188b0b7` انجام شد و همه در کامیت `4faa39e` پاسخ گرفتند.
متن کامل بازبینی در بخش ۷ این سند دست‌نخورده نگه داشته شده.

### ۲.۱ تازگی مظنه — **انجام‌شده**

| زیرخواسته | محل کد | شاهد |
|---|---|---|
| عمر از داده و کش واقعی | `data/order_book.py::OrderBookClient.cache_age_seconds` · `market/tradability_screener.py::_book_age` | `test_quote_age_comes_from_the_real_cache_age_not_a_constant` |
| آستانه واقعاً اثر کند | `market/tradability.py` سنجه‌ی `quote_age` | `test_the_staleness_threshold_actually_changes_the_verdict` — همان دادهٔ ۹۰ ثانیه‌ای با آستانه ۱۲۰ می‌گذرد، با ۳۰ رد می‌شود |
| زمان دریافت ≠ زمان بازار | فیلد `LiquidityObservation.source_time` + `TradabilityReport.source_time_note` + برچسب «عمر دادهٔ دریافتی (نه زمان بازار)» | `test_missing_market_timestamp_is_stated_explicitly` |
| نبودِ عمر ⇒ نامعلوم، نه تازه | `_book_age` برمی‌گرداند `None` | `test_an_unknown_cache_age_is_unknown_not_fresh` |

**قیدِ باقی‌مانده (نه نقص، ولی بدانید):** اگر دفتر چندسطحی در دسترس
نباشد و فقط سطح‌اولِ زنجیره بخوانیم، عمر **نامعلوم** می‌ماند (زنجیره
عمر کش را اعلام نمی‌کند). یعنی آن مسیر همیشه «نیازمند بررسی» می‌دهد.

### ۲.۲ ظرفیت خروج با قیمت قابل‌قبول — **انجام‌شده**

| زیرخواسته | محل کد | شاهد |
|---|---|---|
| نسبت عمق بتواند از ۱ بالاتر برود | `data/order_book.py::OrderBook.real_depth` (جایگزین `fill_price` که بریده می‌شد) | `test_depth_ratio_is_not_capped_at_one` — عمق ۳۰ برای سفارش ۱۰ حالا ۳٫۰ است، قبلاً ۱٫۰ |
| لغزش شدید رد شود | `Thresholds.max_exit_slippage_pct` + `LiquidityObservation.exit_slippage_pct` | `test_depth_far_from_the_touch_does_not_count_as_capacity` — دفتر ۱@۱۰۰۰ + ۱۰۰@۵۰۰: عمق می‌گذرد، لغزش ۴۵٪ رد می‌کند |
| آستانه قابل تنظیم | `config/loader.py` · `/api/tradability` · فرم پنل سیگنال‌ها | `test_a_generous_slippage_threshold_accepts_the_same_book` |
| مبنا در پنل روشن | `TRADABILITY_FIELDS` در `web/api.py`، واحد «٪ فاصله‌ی قیمتِ پرشدن از بهترین مظنه» | تصویر پنل |

**قیدِ باقی‌مانده:** در مسیرِ بدون دفتر چندسطحی، لغزش فقط دو حالت دارد:
صفر (اگر سفارش در سطح اول جا شود) یا `None`. مدلِ دقیق‌تری ندارد چون
داده‌ی بیشتری در دست نیست.

### ۲.۳ تاریخچه‌ی زنده و جلسه‌ی معاملاتی — **انجام‌شده**

| زیرخواسته | محل کد | شاهد |
|---|---|---|
| به‌روزرسانی در طول اجرا | `storage/market_history.py::_load` با امضای `mtime`/اندازه | `test_new_snapshots_are_seen_without_a_restart` |
| پایگاهِ بعداً ساخته‌شده دیده شود | `_connect` دیگر دلیلِ در دسترس نبودن را کش نمی‌کند | `test_a_database_created_later_is_picked_up` |
| روز تقویمی ≠ جلسه | `_trading_sessions` + تزریق `is_trading_day` از `bootstrap.build_tradability_screener` | `test_a_non_trading_day_is_not_counted_as_a_session` |
| نمونه‌ی ۲۰۲۶-۰۹-۱۱ شاهد معامله نیست | `docs/strategy-research.md` بخش ۴ (بلوک «تصحیح مهم») | تأیید مستقل: `date(2026,9,11).weekday()==4` و `TradingCalendar().is_trading_day(...) is False` |
| بدون تقویم ⇒ نامعلوم | `HistoryStats.sessions_verified` | `test_without_a_calendar_the_history_is_unverified` |

**قیدِ باقی‌مانده — مهم:** دقتِ فیلتر به دقتِ `TradingCalendar` بسته
است. تنظیمات پیش‌فرض `learn_from_market: true` است و تعطیلات را از
تاریخچه‌ی نماد مرجع یاد می‌گیرد؛ **بدون شبکه به «فقط آخرهفته» برمی‌گردد**،
پس یک تعطیلیِ رسمیِ وسط هفته ممکن است هنوز جلسه شمرده شود.

### ۲.۴ دقیق‌تر کردن پژوهش — **بخشی انجام‌شده، یک بخش عمداً باز**

| زیرخواسته | وضعیت | جزئیات |
|---|---|---|
| سه محور جدا (شبیه‌ساز / اجرا در ایران / شواهد) | **انجام‌شده** | `docs/strategy-research.md` بخش ۶، جدول چهارستونی |
| حکم وجه تضمین Covered Call و Collar | **انجام‌شده — تصحیح شد** | بخش ۶.۱؛ حکم قبلی نادرست بود |
| هم‌خوانی «فعال/آزمایشی/غیرفعال» با رفتار واقعی | **انجام‌شده** | ستون «وضعیت انتشار» حالا توصیفِ رفتار است؛ «غیرفعال از پیشنهادها» برای covered call حذف شد |
| معنای «اتمیک» | **انجام‌شده** | بخش ۷ سند پژوهش + README: فقط پذیرش/ردِ گروهِ سیگنال |
| **تأیید اعتبار جاری دستورالعمل** | **انجام نشد** | محدودیتش نوشته شد (بخش ۸ بند ۶) |
| **تأیید مشخصات قرارداد هدف** | **انجام نشد** | محدودیتش نوشته شد (بخش ۸ بند ۷) |

دو مورد آخر عمداً باز ماندند: `cmr.seo.ir` و `tse.ir` پاسخ قابل خواندن
ندادند. **منبع یا نتیجه ساخته نشد.**

### ۲.۵ کار نیمه‌تمامِ شناخته‌شده

- **`tradability.default_order_size_contracts` تعریف شده ولی هیچ‌جا
  خوانده نمی‌شود.** در `config/loader.py:196` و
  `config/settings.example.yaml:266` هست؛ `grep` در کد هیچ مصرفی
  نشان نمی‌دهد. غربالگر همیشه از `signal.suggested_qty` استفاده
  می‌کند. یا باید به مسیر «غربال یک نماد بدون سیگنال» وصل شود، یا
  حذف شود. **الان یک کلید مرده است.**

---

## ۳. تصمیم‌های مهم این مرحله

1. **غربال دروازه است، نه امتیاز.** در یک snapshot واقعی، ۱۸٪
   قراردادها اصلاً مظنه‌ی خرید ندارند؛ برای آن‌ها امتیاز معنا ندارد.
   پس غربال **پیش از** امتیازدهی می‌آید و امتیاز بالا جایش را
   نمی‌گیرد.
2. **سه وضعیت، نه دو:** `tradable` / `needs_review` / `rejected`.
   فقط اولی وارد پیشنهادها می‌شود. نبودِ داده هرگز «قبول» ترجمه
   نمی‌شود.
3. **جای اتصال:** `signals/signal_generator.py::_post_process` —
   **پس از** اندازه‌گیری ریسک (چون عمق به تعداد بستگی دارد) و پیش از
   انتشار. چون گروه‌های چندپایه از قبل اتمیک پردازش می‌شدند، ردِ یک
   پایه کلِ گروه را کنار می‌گذارد و رایگان به دست آمد.
4. **تاریخچه فقط‌خواندنی.** `mode=ro` و فایلِ نبوده ساخته نمی‌شود؛
   تست بایت‌به‌بایت بررسی می‌کند. غربال نباید به ثبت خام نشت کند.
5. **سه فیلد به `OptionContract` اضافه شد** (`trade_count`،
   `bid_quantity`، `ask_quantity`) که در پاسخ TSETMC بود و نگاشت
   نمی‌شد — به‌جای گرفتن داده‌ی دوباره.
6. **آستانه‌ها اثبات‌شده نیستند** و هیچ‌جا این‌طور معرفی نشده‌اند.

---

## ۴. پژوهش — تأییدشده در برابر تأییدنشده

گزارش کامل: [`docs/strategy-research.md`](strategy-research.md).

### ✅ تأییدشده (منبع اولیه، با شماره‌ی ماده)

سند: «دستورالعمل معاملات قرارداد اختیار معاملهٔ سهام در بورس اوراق
بهادار تهران و فرابورس ایران»، ۵۱ ماده، مصوب **۱۳۹۱/۱۲/۱۳**، اصلاح
**۱۳۹۵/۰۹/۱۷** —
[PDF](https://www.ifb.ir/Files/RulesRegulations/option.pdf) ·
[صفحه‌ی مقررات فرابورس](https://www.ifb.ir/cms.aspx?tabId=107).

- **ماده ۱۲:** مشخصات قرارداد ۲۱ قلم است و **به ازای هر قرارداد**
  تعیین می‌شود (وجه تضمین، سبک اعمال، کارمزد، نوع تسویه و …).
- **بند ۶۷ + جدول پیوست:** وجه تضمین فقط از **فروشنده**؛ موقعیت باز
  **خرید** اصلاً وجه تضمین نمی‌خواهد.
- **بند ۶۲ + بند ۵-۳ پیوست:** «فروش اختیار خرید **پوشش‌داده‌شده**»
  رسماً شناخته می‌شود و الزامش **مسدود شدن سهم پایه** است، نه تودیع
  نقد. ⚠️ این حکمِ قبلیِ ما را تصحیح می‌کند.
- **بند ۶ پیوست:** درصدهای `A`/`B` فرمول وجه تضمین در **مشخصات
  قرارداد** تعیین می‌شوند، نه در خود دستورالعمل.
- **ماده ۴۴:** قرارداد متوقف، در آخرین روز معاملاتی **با آخرین قیمت
  پایانی سهم پایه** تسویه‌ی نقدی می‌شود.
- **ماده ۱۳:** کارمزد **اعمال** جدا از کارمزد معامله است.
- **داده‌ی خودمان:** اندازه‌ی قرارداد در snapshot ثبت‌شده برای ۱۴۰۴
  نماد ۱۰۰۰ و برای ۲۸ نماد **۱۳۷۱** است → «همه یکسان» قطعاً نادرست.

### ⚠️ تأییدنشده / فرض

- **اعتبار جاری دستورالعمل.** نسخه‌ی ۱۳۹۵ امروز روی سایت فرابورس
  منتشر است، ولی مرجع رسمیِ مقررات (`cmr.seo.ir`) و `tse.ir` پاسخ
  قابل خواندن ندادند. **اگر پس از ۱۳۹۵ اصلاحیه‌ای باشد، در سند ما
  نیست.**
- **مشخصات قرارداد نمادهای هدف** (`ضخود`، `ضهرم` و …): درصدهای
  `A`/`B`، سبک اعمال و کارمزدشان استعلام نشد.
- **هیچ پژوهش عملکردی** با روش و داده درباره‌ی بازدهی استراتژی‌های
  اختیار در ایران پیدا نشد (انگلیسی و فارسی، شامل `jfr.ut.ac.ir`،
  `ensani.ir`، `sid.ir`، `civilica.com`). آنچه فراوان است آموزش و
  تبلیغ است که شاهد نیست. **نبودِ شاهد ≠ رد استراتژی.**
- **آمار نقدشوندگیِ بخش ۴ سند پژوهش** از یک snapshot در **روز
  غیرمعاملاتی** است. پراکندگی را نشان می‌دهد، ولی «فعالیت روزانه» از
  آن استنتاج نمی‌شود.

---

## ۵. وضعیت محلی

**تغییرات کامیت‌نشده: هیچ.** درخت کاری تمیز است، `git stash` خالی.

**پردازه‌ی در حال اجرا: هیچ.** هیچ `python.exe` در اجرا نیست و هیچ
سرور دمویی روی پورت‌های ۸۰۷۷/۸۰۷۸/۸۰۷۹ باز نمانده.

**تست‌ها:** آخرین اجرای کامل محلی **۱۰۷۹ تست پاس**، `ruff` پاس. (روی
همان `4faa39e` که CI هم سبز است.)

### پایگاه‌ها

| پایگاه | وضعیت | توضیح |
|---|---|---|
| `option_signal_bot/var/recorder/market.db` | نسخه ۳ · ۹۸۰ KiB · integrity ok · **۱ snapshot، ۱۴۳۲ quote، فقط روز ۲۰۲۶-۰۹-۱۱** | **دست‌نخورده.** غربالگر فقط‌خواندنی می‌خواندش. آن یک روز **جمعه** بود، پس پس از فیلتر تقویم **صفر جلسه‌ی معاملاتی** دارد. |
| `option_signal_bot/var/paper_trading.db` | نسخه ۳ · ۵۲ KiB · integrity ok · حساب دست‌نخورده (نقد ۱٬۰۰۰٬۰۰۰، `created_at` ۲۰۲۶-۰۹-۱۱T۰۰:۲۴:۳۰) | این سشن از نسخه ۲ به ۳ ارتقا یافت. **پیش از ارتقا پشتیبان سازگار گرفته شد** و حفظ داده مقدارـ‌به‌ـمقدار تأیید شد. حساب **reset نشد**. |
| پشتیبان‌ها | `var/backup-20260911T224029/` (paper trading، پیش از ارتقا) و سه پوشه‌ی `var/recorder/backup-2026*` | ⚠️ بدون اجازه‌ی محمد حذف نشوند. |
| پایگاه‌های قدیمیِ recorder | `market-schema-v1.db`، `market-schema-v2-inscode.db`، `test-schema-v1.db`، `test-schema-v2-key.db` | دست‌نخورده. |

`config/settings.yaml` **وجود ندارد** — پیش‌فرض‌های `config/loader.py`
اعمال می‌شوند. (داشبورد در اولین اجرا از روی `settings.example.yaml`
می‌سازدش.)

### محیط (نکاتی که وقت می‌گیرند)

- مفسر: `D:\Users\M.Kharmandar\AppData\Local\Programs\Python\Python313\python.exe`
  (۳.۱۳.۴، روی PATH). `.venv` وجود ندارد و لازم هم نیست.
- `gh` نصب نیست. PR با `git credential fill` + `curl` به REST API.
- `ruff` نصب نیست؛ venv جدا در scratchpad سشن.
- خروجی فارسی: همیشه `PYTHONIOENCODING=utf-8`.
- خروجی `pytest` را به فایل بفرستید؛ فیلتر RTK گاهی «No tests
  collected» نشان می‌دهد در حالی که تست‌ها پاس شده‌اند.
- ⚠️ **خط‌پایان:** `option_signal_bot/README.md` و
  `data/tsetmc_option_chain_client.py` در git با **CRLF** ذخیره‌اند.
  نوشتنشان با `newline="\n"` کل فایل را در diff عوض می‌کند. راه‌حل:
  با `\r\n` بنویسید یا `git -c core.autocrlf=false add` کنید، و پس از
  هر کامیت با مقایسه‌ی تعداد `\r` بررسی کنید.

---

## ۶. اولین اقدام جلسه‌ی بعد

**منتظر بازبینی کدکس روی `4faa39e` بمانید و تا تعیین تکلیف PR #6
مرحله‌ی بعد را شروع نکنید.** مرحله‌ی امتیازدهی، اخبار و مدل پیش‌بینی
**عمداً شروع نشده‌اند**.

اگر بازبینی هنوز نرسیده، مفیدترین کارِ بدون ریسک به ترتیب:

1. **تاریخچه بسازید.** بزرگ‌ترین مانعِ فعلی این است که صفر جلسه‌ی
   معاملاتیِ تأییدشده داریم، پس غربالگر عملاً هیچ‌وقت «قابل معامله»
   نمی‌دهد. در یک روز معاملاتی (شنبه تا چهارشنبه، ۰۹:۰۰–۱۲:۳۰ تهران):

   ```powershell
   cd D:\Users\M.Kharmandar\Desktop\Codes\optionbaz\Kh-Optionbaz\option_signal_bot
   python scripts\record_market.py --loop
   ```

   پس از چند روز، `min_history_sessions` (پیش‌فرض ۵) تأمین می‌شود و
   سنجه‌ی «تداوم معامله» از «نامعلوم» بیرون می‌آید.

2. **تکلیف `default_order_size_contracts` را روشن کنید** (بخش ۲.۵) —
   یا وصلش کنید یا حذفش.

3. اگر دسترسی به `cmr.seo.ir` یا `tse.ir` باز شد، دو موردِ
   تأییدنشده‌ی بخش ۴ را ببندید.

**اجرای داشبورد** برای دیدن غربالگر (تب «سیگنال‌ها»):

```powershell
cd D:\Users\M.Kharmandar\Desktop\Codes\optionbaz\Kh-Optionbaz\option_signal_bot
python -m web
```

---

## ۷. متن کامل آخرین بازبینی کدکس (روی `188b0b7`)

عیناً و بدون خلاصه‌سازی نگه داشته شده:

> روی همان PR #6 ادامه بده. انتخاب روش پیاده‌سازی با خودت؛ این نتایج
> لازم است:
>
> ۱. تازگی مظنه را از داده و عمر واقعی کش به دست بیاور. زمان دریافت را
> جای زمان بازار معرفی نکن؛ نبود زمان معتبر را صریح نشان بده. آستانه‌ی
> کهنگی باید واقعاً بر تصمیم اثر بگذارد.
>
> ۲. ظرفیت خروج را با قیمت قابل‌قبول بسنج. هم نسبت عمق بیشتر از یک
> درست محاسبه شود، هم سفارش با لغزش شدید صرفاً به‌خاطر وجود حجم در
> قیمت‌های دور پذیرفته نشود. آستانه‌ی لغزش قابل تنظیم و مبنای محاسبه در
> پنل روشن باشد.
>
> ۳. تاریخچه در طول اجرای برنامه به‌روز شود؛ افزودن داده یا
> ساخته‌شدن پایگاهی که ابتدا وجود نداشته، بدون راه‌اندازی مجدد قابل
> مشاهده باشد. روز تقویمیِ ثبت را بدون بررسی، جلسه معاملاتی حساب نکن:
> نمونه‌ی ۲۰۲۶-۰۹-۱۱ جمعه بوده و زمان منبع هم نامعلوم است. آن را شاهد
> «معامله در همان روز» معرفی نکن.
>
> ۴. نتیجه‌ی پژوهش را دقیق‌تر کن: محدودیت شبیه‌ساز، امکان اجرای ساختار
> در ایران و شواهد عملکرد سه موضوع جدا هستند. برای Covered Call و
> Collar، حکم وجه تضمین را از صرف وجود پایه‌ی فروش نتیجه نگیر؛ پوشش
> سهم و قواعد کل ساختار را بررسی کن. اعتبار فعلی دستورالعمل قدیمی و
> مشخصات قرارداد هدف را تأیید کن یا محدودیت تأیید را بنویس.
>
> وضعیت «فعال/آزمایشی/غیرفعال از پیشنهادها» باید با رفتار واقعی انتشار
> و رابط هم‌خوان باشد؛ ردشدن در شبیه‌ساز به‌تنهایی غیرفعال‌شدن پیشنهاد
> نیست. منظور از اتمیک‌بودن هم فقط پذیرش یا رد گروه سیگنال‌هاست، نه
> اجرای اتمیک معاملات.
>
> فقط برای همین خطاهای رفتاری تست هدفمند اضافه کن. نتیجه، نمونه‌های
> قبل و بعد، SHA و CI را گزارش بده. امتیازدهی و اخبار را هنوز شروع
> نکن؛ merge هم نکن.
