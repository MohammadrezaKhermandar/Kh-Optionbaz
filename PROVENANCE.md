# منشأ کد (Provenance)

این مخزن ادامه‌ی توسعه‌ی یک پروژه‌ی موجود است. این سند می‌گوید کد از کجا
آمده و وضعیت **واقعی** اعلان مجوز در نقطه‌ی انتقال چه بوده — بدون افزودن
یا تفسیر چیزی که در مبدأ نبوده.

## مخزن و کامیت مبنا

| مورد | مقدار |
|---|---|
| مخزن مبدأ | <https://github.com/OmidRafiee/GarnetTrader> |
| نام پروژه‌ی مبدأ | GarnetTrader |
| شاخه‌ی مبدأ | `Master` |
| **کامیت مبنا (SHA)** | `689c6c2303e2c1e3834ca9bed8b350846954f428` |
| پیام کامیت مبنا | `Merge pull request #9 from OmidRafiee/feat/paper-trading` |
| تاریخ کامیت مبنا | 2026-09-09 |
| `tree` کامیت مبنا | `4cacfbb5f1cb8246de244118ecd67d6c9b4a2445` |
| نسخه‌ی اعلام‌شده در `pyproject.toml` | `0.5.0` |

شاخه‌ی `main` این مخزن دقیقاً روی همان کامیت مبنا ساخته شده و **تمام
تاریخچه‌ی اجدادی** آن را حفظ می‌کند. هیچ کامیتی بازنویسی، squash یا
rebase نشده است.

## نویسنده‌ی اعلام‌شده

`option_signal_bot/pyproject.toml` در کامیت مبنا:

```toml
authors = [{ name = "Omid Rafiee" }]
```

تنها نویسنده در کل تاریخچه‌ی Git (۶۲ کامیت):

```
Omid Rafiee <omid.green.b13@gmail.com>
```

## وضعیت واقعی اعلان مجوز

این بخش عمداً فقط **ثبت وضعیت** است، نه تفسیر آن.

**آنچه در کامیت مبنا وجود دارد:**

`option_signal_bot/pyproject.toml` خط ۲۱ اعلان مجوز دارد:

```toml
license = { text = "MIT" }
```

**آنچه در کامیت مبنا وجود ندارد:**

- هیچ فایل مستقل `LICENSE`، `LICENCE`، `COPYING` یا `NOTICE` در هیچ‌جای
  درخت وجود ندارد (بررسی‌شده با `git ls-tree -r`).
- در نتیجه، **متن کامل مجوز MIT** و **خط کپی‌رایت** آن
  (`Copyright (c) <سال> <دارنده‌ی حق>`) در مخزن مبدأ موجود نیست.

**ابهام باقی‌مانده (نیازمند تصمیم مالک محصول):**

مجوز MIT برای بازتوزیع، حفظ «اعلان کپی‌رایت و اعلان اجازه» را شرط
می‌کند. در مبدأ اعلانِ نوع مجوز هست ولی متن اعلان و دارنده‌ی حق ثبت
نشده، پس چیزی برای نقل‌کردن عیناً وجود ندارد.

در این PR **هیچ فایل LICENSE ساخته نشده** و هیچ متن مجوز، سال کپی‌رایت
یا نام دارنده‌ی حق **جعل نشده است**. حل این مورد نیازمند یکی از این‌هاست:

1. افزودن `LICENSE` با متن MIT در مخزن **مبدأ** توسط نویسنده‌ی اصلی، و
   سپس هم‌گام‌سازی اینجا؛ یا
2. تأیید کتبی نویسنده‌ی اصلی درباره‌ی سال و دارنده‌ی حق، تا `LICENSE`
   اینجا با همان مقادیر ساخته شود.

تا آن زمان، اعلان موجود (`license = { text = "MIT" }` در `pyproject.toml`)
دست‌نخورده باقی می‌ماند.

## آنچه منتقل شد و آنچه نشد

**منتقل شد:** شاخه‌ی `main` روی کامیت مبنا با کل تاریخچه‌ی اجدادی.

**منتقل نشد (عمدی):**

- شاخه‌های موضوعیِ قدیمی مبدأ (`feat/emofid-api-discovery`،
  `feat/paper-trading`) — هر دو در کامیت مبنا merge شده‌اند و ارجاعشان
  لازم نیست.
- `refs/pull/*` مبدأ.
- هیچ نشست کارگزاری، توکن، فایل HAR، `config/settings.yaml`، محتویات
  `var/` یا داده‌ی حساب شخصی — هیچ‌کدام از این‌ها **هرگز** در تاریخچه‌ی
  مبدأ کامیت نشده بودند (بررسی محتوایی روی کل تاریخچه انجام شد؛ نتیجه در
  گزارش مرحله‌ی انتقال).

## قاعده‌ی جهت تغییرات

مخزن مبدأ **فقط خواندنی** است. هیچ تغییری از این مخزن به مبدأ push
نمی‌شود. در تنظیمات محلی، ریموت `upstream` عمداً بدون آدرس push معتبر
تعریف شده است.
